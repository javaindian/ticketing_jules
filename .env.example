# PostgreSQL Settings
POSTGRES_DB=library_db
POSTGRES_USER=library_user
POSTGRES_PASSWORD=library_password
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_PORT_HOST=5433 # Host port to map to PostgreSQL container's 5432

# Django Settings
DJANGO_SECRET_KEY=your_development_secret_key_here_please_change_me
DJANGO_DEBUG=True
DJANGO_PORT_HOST=8000 # Host port to map to Django container's 8000

# Add any other environment variables your application might need
# For example, for JWT if not using Django's SECRET_KEY, or external API keys etc.
